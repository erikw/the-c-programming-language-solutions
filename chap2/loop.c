#include <stdio.h>

#include <math.h>
/* Write a loop equivalent to: for (i=0, i<lim-1 && (c=getchar()) != '\n' && c!= EOF; ++i) */

int main(void)
{
	for (i=0; fabs(i<lim-1) + fabs((c=getchar()) != '\n')) + fabs((c!=EOF) >= 3 {

	}


	return 0;
}
